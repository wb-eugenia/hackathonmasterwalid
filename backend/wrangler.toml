name = "hackathonwalid"
main = "src/worker.js"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Configuration Cloudflare D1
[[d1_databases]]
binding = "DB"
database_name = "hackathondb"
database_id = "55a73d96-9dda-4d4a-a585-68df69080ebf"
# Variables d'environnement (pour développement local)
[vars]
NODE_ENV = "production"

# Secrets (à configurer via wrangler secret put)
# JWT_SECRET
# GOOGLE_PLACES_API_KEY
# OPENAI_API_KEY (optionnel)
# FRONTEND_URL (pour les emails de vérification)

# Routes (pour Workers)
routes = []
